services:
  db:
    image: postgres:17
    container_name: plumbing-db
    environment:
      POSTGRES_DB: plumbing
      POSTGRES_USER: plumbing
      POSTGRES_PASSWORD: plumbing
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
